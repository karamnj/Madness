<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <Preference
        android:title="Login"
        android:summary="Presenter Login">

        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.sirius.madness"
            android:targetClass="com.sirius.madness.ui.activities.LoginActivity"
            />

    </Preference>
    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <!--<ListPreference
        android:key="role_list"
        android:title="@string/pref_title_select_role"
        android:defaultValue="1"
        android:entries="@array/pref_role_list_titles"
        android:entryValues="@array/pref_role_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />-->

    <!--&lt;!&ndash; NOTE: EditTextPreference accepts EditText attributes. &ndash;&gt;
    &lt;!&ndash; NOTE: EditTextPreference's summary should be set to its value by the activity code. &ndash;&gt;
    <EditTextPreference
        android:key="role_text"
        android:title="@string/pref_title_password"
        android:selectAllOnFocus="true"
        android:inputType="textCapWords"
        android:capitalize="words"
        android:singleLine="true"
        android:maxLines="1" />-->

</PreferenceScreen>
